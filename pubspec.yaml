name: all_exit_codes
description: Just terminal exit codes with more description to use in dart
version: 2.1.1
repository: https://github.com/insign/dart_all_exit_codes
homepage: https://github.com/insign/dart_all_exit_codes

environment:
  sdk: '>=2.17.0 <3.2.0'

platforms:
  linux:
  macos:
  windows:

dev_dependencies:
  lints: ">=3.0.0 <5.0.0"
  test: ^1.21.0
